- const iconsInfo = typeof icons === 'string' ? JSON.parse(icons) : icons
- const iconsMails = typeof iconsEmail === 'string' ? JSON.parse(iconsEmail) : iconsEmail

doctype html
html(lang="es")
  include ./partials/head.pug
  body
    .container
      include ./partials/header.pug
      .content
        div(style="display: flex; justify-content: center; align-items: center; flex-direction: column;")
          img(src=iconsInfo.warning alt=iconsInfo.warning)
          p.title(style="margin-block-end: 0.5em; margin-block-start: 0.5em;") Alerta de Seguridad
        p 
           Estimado/a #{vars.name},
        p 
           Te informamos que hubo un inicio de sesión sospechoso desde otro dispositivo:

        div(style="padding: 20px; background-color: #F8F8F8; border-radius: 10px; margin: 20px 0;")
          div(style="display: flex; justify-content: space-between; margin-bottom: 10px;")
            p(style="margin: 0;") Fecha y hora:
            p(style="margin: 0;") 14 de Julio del 2024, 17:55h
        div(style="display: flex; justify-content: space-between; margin-bottom: 10px;")
          p(style="margin: 0;") Dispositivo:
          p(style="margin: 0;") iPhone (iPhone 14,7)
        div(style="display: flex; justify-content: space-between; margin-bottom: 10px;")
          p(style="margin: 0;") Ubicación:
          p(style="margin: 0;") Cerca de Munich, Baravia, Alemania
        p
           Si fuiste tu, ignora este mail.

        div(style="padding: 1em 0em;")
          hr

        p
          
            strong ¿No reconoces esta actividad?

        p
           Cambia tu contraseña inmediatamente para evitar accesos no autorizados y mejorar la seguridad de tu cuenta.

        div(style="display: flex; justify-content: center; align-items: center; gap: 0.5em;")
          a.button(href="#" target="_blank" rel="noopener noreferrer") Enlace para cambiar contraseña
          a.button(href="#" target="_blank" rel="noopener noreferrer") Contactar con el servicio al cliente

      